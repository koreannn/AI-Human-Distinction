# 🧩 src

> 핵심 모듈 (학습 로직)

## 📂 구조

```
src/
├── training/
│   ├── trainer.py      # Train 클래스
│   └── dataset.py      # DataSet 클래스
└── utils/
    └── config.py       # 설정 로더
```

> ⚠️ Colab 학습 시에는 `scripts/` 스크립트 사용을 권장합니다.
